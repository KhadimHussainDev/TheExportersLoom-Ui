export const SearchManufacturerStyle = (width, height) => ({
  container: {
    flex: 1,
    padding: 10,
  },
  fixedButtonRow: {
    height: height * 0.055, // Dynamic height
    justifyContent: "center",
    marginBottom: height * 0.02,
  },
});
